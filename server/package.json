{
  "name": "server",
  "version": "1.0.0",
  "description": "Realtime sync server for Atelier — Socket.IO broadcast of UI state and cursors",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "socket.io": "^4.8.1"
  }
}
